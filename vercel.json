{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "scripts": {
    "install": "npm install --legacy-peer-deps",
    "build": "npm run build",
    "dev": "npm run start"
  }
}
